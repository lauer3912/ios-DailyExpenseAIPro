name: TestFlight Upload

on:
  workflow_dispatch:

jobs:
  build-and-upload:
    runs-on: macos-26
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: List profiles
        run: |
          ls -la .github/profiles/
          echo "Test passed"
